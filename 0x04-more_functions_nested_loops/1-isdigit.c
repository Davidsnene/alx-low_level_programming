#include "main.h"

/**
 * _isdigit - Entry point
 * @c: variable to print
 *
 * Description: print 1 if digit otherwise 0
 *
 * Return: Nothing
 */
int _isdigit(int c)
{
	if (c => 48 && c <= 57)
	{
	return (1);
	}
return (0);
}
