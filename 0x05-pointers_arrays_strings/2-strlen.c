#include "main.h"

/**
* _strlen - Entry point
* @*s: pointer
* @s: the tring
*
* Description: retuns the lenght of a string
*
* Return: Nothing
*/

void _strlen(char *s)
{
	int i;

	i = 0;
	while (s[i])
	{
		i++;

	return (i);
	}
}
